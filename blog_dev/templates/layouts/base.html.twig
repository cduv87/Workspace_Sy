<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="assets/css/style.css">
    <title>{% block titre %} Titre Défaut {% endblock %}</title>

</head>

<body>
    {# Création d'une variable sur twig #}
    {% set menu = ["Accueil","Blog","Contact","A propos"] %}
    <div class="container-fluid">
        <header>
            <nav class="navbar navbar-expand-lg bg-light">
                <div class="container-fluid"> <a class="navbar-brand" href="#">Navbar</a> <button class="navbar-toggler"
                        type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav"
                        aria-expanded="false" aria-label="Toggle navigation"> <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav">
                            {# début boucle #}
                            {% for ele in menu %}
                            {% if ele != "Accueil" %}
                            <li class="nav-item">
                                <a class="nav-link active" aria-current="page"
                                    href="{{ '/' ~ ele|lower|replace({' ':''})}}">{{ele}}</a>
                                    {#  alternative : href="{{ '/' ~ path(ele)}}">{{ele}}</a> #}
                            </li>
                            {% else %}
                            <a class="nav-link active" aria-current="page" href="/">{{ele}}</a>
                            {% endif %}
                            {% endfor %}
                            {# fin boucle #}
                        </ul>
                    </div>
                </div>
            </nav>
        </header>


        <main>

            {% block main %}


            {% endblock %}

        </main>

        {% include 'layouts/footer.html.twig' %}

    </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"></script>

</body>

</html>